#multi_session.yaml

multi_session:

  sessions_dir: "/home/ian/catkin_ws/seu/" 

  central_sess_name: "01" # should be same as the directory name of that session
  query_sess_name: "02" # should be same as the directory name of that session
  
  save_directory: "0102/" 

  iteration: 5
